import Button from '@/app/components/Button';
import React from 'react';

const page = () => {
    // throw new Error("error")
    return (
        <div className="bg-blue-500 text-white p-4 rounded">
            this is abot mission

            <Button />
        </div>
    );
};

export default page;